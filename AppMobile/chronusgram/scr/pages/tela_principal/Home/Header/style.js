import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    header: {
        flex: 1,
        maxHeight: 300,
        flexDirection: 'row',
        backgroundColor: '#AD96CD',
    },
  });

export default styles
