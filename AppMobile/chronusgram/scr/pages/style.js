import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#16041B',
        alignContent: 'center',
        alignItems: 'center',
      },
    text: {
      top: 240,
      fontSize: 10,
      fontWeight: 'bold',
      color: '#FFF',
      marginVertical: 5, // Espaçamento vertical entre os textos
    },
  });

export default styles