import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 'auto',
        // backgroundColor:'#fff',
        alignContent: 'center',
        alignItems: 'center',
        marginTop: 60,
        marginBottom: 5,
    },
    Logo:{
        width: 200,
        height: 200,
    }
  });

export default styles